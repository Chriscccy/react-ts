import { request } from "./request";
import { getToken, setToken, removeToken, removeTokenRole } from "./token";
import { errNotify, okNotify } from "./notification";
import { getRole, setRole, removeRole } from "./role";
import {
  setVerifyDevice,
  getVerifyDevice,
  removeVerifyDevice,
} from "./verifyDevice";

export {
  request,
  getToken,
  setToken,
  removeToken,
  removeTokenRole,
  errNotify,
  okNotify,
  getRole,
  setRole,
  removeRole,
  setVerifyDevice,
  getVerifyDevice,
  removeVerifyDevice,
};
